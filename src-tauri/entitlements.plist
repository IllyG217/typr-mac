<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS entitlements for Typr.
  Typr must NOT be sandboxed — it needs:
    - Global keyboard shortcuts (Accessibility)
    - Global mouse event grab via rdev (Accessibility)
    - osascript to simulate Cmd+V paste (Accessibility / Apple Events)
  On first launch macOS prompts for Accessibility access in
  System Settings > Privacy & Security > Accessibility.
  Microphone access is requested automatically on first recording.
-->
<plist version="1.0">
<dict>
  <!-- Microphone access for audio recording -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
  <!-- Allow Apple Events for osascript Cmd+V paste simulation -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
  <!-- Hardened runtime — required for notarization -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <false/>
  <!-- NOT sandboxed: com.apple.security.app-sandbox intentionally omitted -->
</dict>
</plist>
